import React from 'react';
import loader from './loader.gif';

const Loader = () => <img src={loader} alt="Loading..." style={{ width: '150px', margin:'auto', display:'block' }} />

export default Loader
